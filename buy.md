```tasks
due before today
path does not include Templates 
not done
has due date
tag includes buy
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```