## Today
```tasks
due today
path does not include Templates 
not done
has due date
tag includes chore
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```

## Upcoming
```tasks
due after today
path does not include Templates 
not done
has due date
tag includes chore
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```

## Overdue

```tasks
due before today
path does not include Templates 
not done
has due date
tag includes chore
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```


## No Due Date
```tasks
path does not include Templates 
not done
no due date
tag includes chore
```

## Done
```tasks
done
path does not include Templates 
tag includes chore
sort by done reverse
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```