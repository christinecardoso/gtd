## Today
```tasks
due today
path does not include Templates 
not done
has due date
(tag includes cook) OR (tag includes kitchen) OR (tag includes mealprep)
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```


```tasks
due today
path does not include Templates 
path includes Kitchen
not done
has due date
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```
## Upcoming
```tasks
due after today
path does not include Templates 
not done
has due date
(tag includes cook) OR (tag includes kitchen) OR (tag includes mealprep)
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```

## Overdue

```tasks
due before today
path does not include Templates 
not done
has due date
(tag includes cook) OR (tag includes kitchen) OR (tag includes mealprep)
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```


## No Due Date
```tasks
path does not include Templates 
not done
no due date
(tag includes cook) OR (tag includes kitchen) OR (tag includes mealprep)
```

## Done
```tasks
done
path does not include Templates 
(tag includes cook) OR (tag includes kitchen) OR (tag includes mealprep)
sort by done reverse
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```


## Recurring
```tasks
path includes Kitchen 
not done
has due date
is recurring
```

## Reschedule
```tasks
due before today
path includes Kitchen 
not done
has due date
sort by due reverse
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
is not recurring
sort by function task.due.format("dddd")
```


## Schedule
```tasks
path includes Kitchen 
not done
no due date
```