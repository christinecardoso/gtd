```tasks
due after today
path does not include Templates 
not done
has due date
sort by path
sort by tag
sort by status.name
sort by urgency
sort by priority
sort by status.type
sort by function task.due.format("dddd")
```